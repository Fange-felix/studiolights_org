# studiolights_org 仓库技术交底

## 一、仓库概述

- **仓库地址**：https://github.com/Fange-felix/studiolights_org
- **GitHub账号**：Fange-felix
- **仓库用途**：管理 WordPress 网站的 Google SEO 文章，JSON 格式输出
- **目标CMS**：WordPress
- **SEO引擎**：Google
- **文章编号前缀**：SL-XXX

---

## 二、目录结构

```
studiolights_org/
├── 草稿/              ← 新文章写好放这里（按行业分子目录）
├── 待入库/            ← 审核通过、准备入库的文章
├── 已发布/            ← 已入库到 WordPress 的文章
├── 作废/              ← 废弃不用的文章
├── 文章模板.json      ← 新文章统一模板（复制后填写）
├── 文章索引.csv       ← 全部文章汇总表
├── 仓库技术交底.md    ← 本文档
└── README.md          ← GitHub 自带
```

### 文章流转流程

```
草稿 → 待入库 → 已发布
                ↘ 作废
```

1. 写手在 `草稿/` 目录创建文章 JSON
2. 审核通过后移动到 `待入库/` 目录
3. 入库到 WordPress 后移动到 `已发布/` 目录
4. 废弃文章移动到 `作废/` 目录

---

## 三、文章 JSON 格式说明

每篇文章为一个独立 JSON 文件，文件名格式：`SL-XXX.json`（如 `SL-001.json`）

### 字段结构

```json
{
  "article_id": "SL-001",
  "industry": "行业名称",
  "classid": 1,
  "status": "草稿",
  "created_at": "2026-07-26",
  "updated_at": "2026-07-26",
  "imported_to_db": false,
  "db_id": null,
  "meta_info": {
    "primary_keyword": "主关键词",
    "secondary_keywords": ["副关键词1", "副关键词2"],
    "search_intent": "搜索意图",
    "target_engine": "Google",
    "title_tag": "SEO标题（≤60字符）",
    "meta_description": "Meta描述（≤160字符）",
    "url_slug": "url-slug"
  },
  "content": {
    "h1": "文章H1标题",
    "word_count": 0,
    "h2_sections": [
      {"h2": "章节标题", "content": "章节正文"}
    ],
    "html_content": "<h1>标题</h1><h2>章节</h2><p>正文</p>"
  },
  "images": {
    "title_pic": "",
    "content_images": []
  },
  "seo_checklist": {
    "title_length_chars": 0,
    "meta_description_length_chars": 0,
    "primary_keyword_in_h1": true,
    "primary_keyword_in_first_paragraph": true,
    "h2_count": 0,
    "mobile_optimized": true,
    "original_content": true
  },
  "notes": "备注"
}
```

---

## 四、JSON 字段与 WordPress 字段映射

| JSON 字段 | WordPress 对应 | 说明 |
|-----------|---------------|------|
| `content.html_content` | `post_content` | 文章正文HTML |
| `content.h1` | `post_title` | 文章标题 |
| `meta_info.title_tag` | Yoast/RankMath SEO Title | SEO标题 |
| `meta_info.meta_description` | Yoast/RankMath Meta Description | SEO描述 |
| `meta_info.url_slug` | `post_name` | URL别名 |
| `classid` | WordPress 分类ID | 对应 wp_terms.term_id |
| `meta_info.primary_keyword` | Yoast Focus Keyword | 重点关键词 |
| `meta_info.secondary_keywords` | Yoast Additional Keywords | 副关键词 |
| `images.title_pic` | 特色图片 | Featured Image |
| `status` | `post_status` | draft/publish |
| `industry` | 分类名称 | 可作为 category |

---

## 五、WordPress 入库方案

### 方案A：WordPress REST API（推荐）

通过 WordPress REST API 批量创建文章，无需登录后台。

**前提条件**：
- WordPress 网站已启用 REST API（默认开启）
- 创建 Application Password：后台 → 用户 → 个人资料 → 应用密码
- 或使用 JWT Auth 插件

**API 端点**：
```
POST https://你的域名/wp-json/wp/v2/posts
```

**请求头**：
```
Authorization: Basic base64(用户名:应用密码)
Content-Type: application/json
```

**请求体示例**：
```json
{
  "title": "文章H1标题",
  "content": "<h1>标题</h1><h2>章节</h2><p>正文</p>",
  "slug": "url-slug",
  "status": "draft",
  "categories": [1],
  "meta": {
    "_yoast_wpseo_title": "SEO标题",
    "_yoast_wpseo_metadesc": "SEO描述",
    "_yoast_wpseo_focuskw": "主关键词"
  }
}
```

### 方案B：WP-CLI 命令行导入

如果可以 SSH 到服务器，用 WP-CLI 批量导入：

```bash
wp post create \
  --post_title='文章标题' \
  --post_content='HTML正文' \
  --post_name='url-slug' \
  --post_status=draft \
  --post_category=1 \
  --allow-root
```

### 方案C：WP All Import 插件

1. 安装 WP All Import 插件
2. 上传 JSON 文件
3. 拖拽映射 JSON 字段到 WordPress 字段
4. 执行导入

---

## 六、入库操作流程

```
1. 从 GitHub 拉取最新文章
   → 访问 https://github.com/Fange-felix/studiolights_org
   → 下载 待入库/ 目录下的 JSON 文件

2. 执行入库脚本
   → 读取 JSON 文件
   → 调用 WordPress REST API 创建文章
   → 记录返回的 post_id 到 JSON 的 db_id 字段

3. 入库后处理
   → 将 JSON 文件从 待入库/ 移动到 已发布/
   → 更新 文章索引.csv 中「是否入库」列为「是」
   → 更新 JSON 中 imported_to_db 为 true

4. WordPress 后台检查
   → 确认文章已创建为草稿
   → 检查特色图片、分类、SEO元数据
   → 确认无误后发布
```

---

## 七、Google SEO 注意事项

| 项目 | Google 要求 | 与百度的区别 |
|------|------------|-------------|
| 标题长度 | 50-60字符 | 百度≤30字符 |
| Meta描述 | 150-160字符 | 百度≤78字符 |
| URL结构 | 短、含关键词、英文slug | 百度可中文URL |
| 内容长度 | 1500-2000字为佳 | 百度500-800字也可 |
| 结构化数据 | JSON-LD Schema | 百度也有但标准不同 |
| 站长工具 | Google Search Console | 百度站长平台 |
| 统计工具 | Google Analytics | 百度统计 |
| 移动适配 | 响应式优先 | 百度MIP |
| 内链策略 | 语义相关内链 | 百度也重视 |
| 外链 | 高质量自然外链 | 百度生态内链更重要 |

---

## 八、文章索引 CSV 格式

文件 `文章索引.csv` 包含以下列：

| 列名 | 说明 |
|------|------|
| 文章编号 | SL-XXX |
| 主关键词 | SEO主关键词 |
| 副关键词 | 用竖线分隔 |
| 行业 | 所属行业 |
| 栏目ID | WordPress分类ID |
| 文章状态 | 草稿/待入库/已发布/作废 |
| 存放路径 | 在仓库中的路径 |
| 是否入库 | 是/否 |
| 入库ID | WordPress post_id |
| 创建日期 | YYYY-MM-DD |
| 更新日期 | YYYY-MM-DD |
| URL_Slug | 英文URL别名 |

---

## 九、协作说明

- **写手（小吴/AI）**：负责撰写文章 JSON，放入 `草稿/` 目录
- **入库（小海/AI 或人工）**：从 `待入库/` 读取 JSON，通过 API 入库到 WordPress
- **审核**：文章入库前需人工确认内容质量和 SEO 合规

---

## 十、与其他仓库的对比

| 项目 | br_wz | studiolights_org |
|------|-------|------------------|
| 目标CMS | 帝国CMS | WordPress |
| SEO引擎 | 百度 | Google |
| 文章编号 | BR-XXX | SL-XXX |
| 网站域名 | brtycl.com | 待确认 |
| 入库方式 | PHP脚本直接写MySQL | REST API / WP-CLI |
| 栏目ID | 14=公司动态, 15=行业动态 | WordPress分类ID（待配置） |

---

*文档更新日期：2026-07-26*
*维护人：小吴*
